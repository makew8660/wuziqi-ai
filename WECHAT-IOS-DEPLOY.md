# iPhone 微信打开说明

## 能不能做

可以。

最合适的形态不是 iOS App，而是把当前页面部署成一个 `HTTPS H5 网页`，然后把链接发到微信里。  
iPhone 用户在微信里点开后，直接就是微信内置浏览器打开。

## 你还差什么

1. 一个可访问的 `https://` 域名
2. 把当前目录上传到服务器或静态托管

只要能访问 `https://你的域名/index.html`，就已经可以在微信里分享并打开。

## 如果你只是想“能打开”

这一步不用接微信 JS-SDK。  
直接分享网页链接就行。

## 如果你还想“分享卡片更好看”

你会需要：

1. 部署后的真实网址
2. 微信服务号后台配置
3. JS-SDK 和 `JS接口安全域名`

这样才能稳定自定义：

- 分享标题
- 分享描述
- 分享缩略图

## 当前文件

- 页面入口：`index.html`
- 分享封面：`share-cover.svg`
- H5 manifest：`manifest.webmanifest`

## 注意

- 本地 `file://` 文件不能直接拿去微信分享给别人打开
- 微信里分享，必须是公网可访问链接
- 如果后面要接 JS-SDK，建议把分享图改成真实 `png/jpg`，并在页面里换成部署后的绝对链接
